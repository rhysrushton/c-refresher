#include <stdio.h>
#include <string.h>

int main() {

  //try to fix the first problem that the compiler is telling you.
  //common problems in code
  /*
    logic errors
    syntax erros
    memory corruption
    performance scalability
    lack of cohesion
    tight coupling (lots of dependencies between functions/objects)
    
    understand the problem -> don't just dive in. 
     
    
  */
  // Tsasasashe call stack
  // x
  return 0; 
}
